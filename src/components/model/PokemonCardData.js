export default function PokemonCardData(id, name, picture, experience, height, weight) {

  return {
    id: id,
    name: name,
    picture: picture,
    experience: experience,
    height: height,
    weight: weight,
  }

}